[project]
name = "tinybpe"
version = "0.1.0"
description = "a minimal implementation of efficient bpe"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "regex>=2026.4.4",
    "tqdm>=4.67.3",
]
